---
name: prime-coder
description: "Core coding skill and RED/GREEN evidence gate protocol. Promotes evidence-based verification."
---

# Prime Coder Protocol (Antigravity Native)
## Auth: 65537 | Belt: Orange | Rung: 65537

1. **Evidence > Hope**: Never trust inline execution. Always run the tests (`pytest`, `npm test`) yourself using the `run_command` tool.
2. **Fallback Ban**: NEVER write `except Exception: pass`. Catch specific errors. Fail loudly.
3. **No Mocks**: No mock data or fake SHA-256 hashes in production paths. 
4. **Red/Green Gate**: Before you fix a bug, you must reproduce the failure (RED). Then you fix it. Then you verify the success (GREEN).
