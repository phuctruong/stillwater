# USER

Default user profile:
- wants local-first operation with optional remote model routing
- values reproducibility, receipts, and transparent tradeoffs
- expects easy overrides through files, not brittle code edits

User-adjustable knobs:
- model/provider settings
- extension root and skill/recipe directories
- persona and identity overlays

