# Swarm Settings

Use `SWARM-ORCHESTRATION.prime-mermaid.md` as the external swarm config contract.

Override order:
1. `STILLWATER_SWARM_SETTINGS_FILE`
2. `swarm_settings_file` in `src/cli/kernel_config.yaml`
3. `src/cli/extensions/settings/SWARM-ORCHESTRATION.prime-mermaid.md` (if present)
4. `src/cli/settings/SWARM-ORCHESTRATION.prime-mermaid.md`
